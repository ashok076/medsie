import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    row: {
        flexDirection: 'row',
    },
    rowContainer: {
        width: '50%'
    },
    viewMargin: {
        marginTop: 5
    },
    close: {
        color: 'red'
    }
})

export default styles