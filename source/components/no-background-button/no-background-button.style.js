import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    showMap: {
        borderWidth: 0.7,
        borderColor: '#000000',
        padding: 5,
        borderRadius: 5,
        margin: 1
    }
})

export default styles